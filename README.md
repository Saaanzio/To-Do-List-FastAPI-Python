# Small FastAPI project for learning purposes.
# Requirements
pip install fastapi
pip install uvicorn
# How to run
uvicorn main:app --reload
your localhost port with docs -> http://127.0.0.1:8000/docs#/
